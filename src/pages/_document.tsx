import { Html, Head, Main, NextScript } from "next/document";

const Document = () => (
	<Html lang="en">
		<Head />
		<body className="bg-neutral-800 text-zinc-200">
			<Main />
			<NextScript />
		</body>
	</Html>
);

export default Document;
